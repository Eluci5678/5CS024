{% include 'header.twig' %}
{% include 'top-bar.twig'%}

<body>
    <a href="index.php">home</a>

    <table>
        <tr>
            <th>Rank</th>
            <th>Name</th>
            <th>Attendance</th>
        </tr>

        {% for u in users %}
            <tr>
                <td>{{ u.rank }}</td>
                <td>{{ u.name }}</td>
                <td>{{ u.attendance_score }}</td>
            </tr>
        {% endfor %}
    </table>
</body>

{% include 'footer.twig' %}
