{% include 'header.twig' %}
{% include 'top-bar.twig'%}

<body>
    <div class="main">
        <a href="index.php" class="btn btn-primary" style="margin-bottom: 5px;">Home</a>

        <table class="table table-striped table-bordered table-hover">
            <thead>
                <tr>
                    <th>Rank</th>
                    <th>Name</th>
                    <th>Attendance</th>
                </tr>
            </thead>

            <tbody>
                {% for u in users %}
                    <tr>
                        <td>{{ u.rank }}</td>
                        <td>{{ u.name }}</td>
                        <td>{{ u.attendance_score }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</body>

{% include 'footer.twig' %}
