<nav class="navbar navbar-dark bg-dark px-3">
    <div class="HomeLink">
        <a class="navbar-brand mb-0 h1" href="index.php">Student Portal</a>
    </div>

    <div class="d-flex align-items-center gap-2">
        {% if user %}
            <a class="btn btn-primary" href="dashboard.php">Dashboard</a>
            <div class="dropdown">
                <a class="nav-link dropdown-toggle text-white" href="#" style="padding: 6px 0px 6px 0px" role="button" id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false">{{ user.name }}</a>
                <ul class="dropdown-menu dropdown-menu-end dropdown-menu-dark" aria-labelledby="userDropdown">
                    <li><a class="dropdown-item" href="profile.php">Profile</a></li>
                    <li><hr class="dropdown-divider" style="margin:  4px 0px 4px 0px;"></li>
                    <li><a class="dropdown-item" href="php/logout.php">Logout</a></li>
                </ul>
            </div>
        {% else %}
            <a class="btn btn-primary" href="sign-up.php">Sign up</a>
            <a class="btn btn-outline-light" href="login.php">Login</a>
        {% endif %}
    </div>
</nav>