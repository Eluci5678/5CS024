{% include 'header.twig' %}
{% include 'top-bar.twig' %}

<body>
    <a href="index.php">home</a>

    <h1>Joined Clubs</h1>
    {% for club in clubs %}
        <p>{{ club.club_name }}</p>
    {% endfor %}
    <h1>Joined Events</h1>
    {% for event in events %}
        <p>{{ event.title }}</p>
    {% endfor %}
</body>

{% include 'footer.twig' %}